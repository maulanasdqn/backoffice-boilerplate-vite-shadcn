import { FC, ReactElement } from "react";

export const Sidebar: FC = (): ReactElement => {
  return (
    <aside className="flex flex-col w-full max-w-52 h-screen bg-gray-400">
      Sidebar
    </aside>
  );
};
